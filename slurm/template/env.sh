## add commands to this file that make gcc, parallel, and julia available.

## if using lmod, grab the latest versions
module load gcc parallel julia

## if using bashrc, source it
# source $HOME/.bashrc

## if using manually installed software, make aliases
# alias julia = "/full/path/to/julia/bin/julia"
# alias gcc = "/full/path/to/gcc/bin/gcc"
# alias parallel = "/full/path/to/parallel/bin/parallel"